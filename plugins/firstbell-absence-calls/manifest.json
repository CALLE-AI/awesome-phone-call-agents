{
  "name": "firstbell-absence-calls",
  "title": "n8n school-absence call wave, in each family's language",
  "platform": "n8n",
  "type": "workflow-template",
  "description": "Inactive-by-default n8n workflow that calls the families whose absence notification went unanswered, each in that family's own language, and keeps three outcomes apart: a usable answer, a conversation that produced nothing, and nobody reached. Ships with a dry run that places no calls.",
  "entrypoints": [
    {
      "name": "Call the families whose absence is still unexplained, in their own language",
      "path": "examples/absence-wave.workflow.json"
    }
  ],
  "side_effects": [
    "Creates outbound phone calls only after CALL_E_API_KEY is configured, the shipped placeholder numbers are replaced, and dryRun is set to false.",
    "Placing a call is not reversible. The CALL-E API has no cancel endpoint, so a call already accepted will run to completion even after the workflow is deactivated."
  ],
  "safety": [
    "Imports inactive and runs from its Manual Trigger. The schedule node ships disabled.",
    "dryRun is true on import, so the first end-to-end run places no calls and needs no API key.",
    "Refuses to dial the shipped placeholder numbers, which use the +91 555 range that cannot be assigned to a subscriber.",
    "Validates every destination number as E.164 before the wave starts, rather than failing halfway through it.",
    "Consent is never inferred. A row without recorded consent is refused rather than defaulted, and is reported as skipped rather than counted among the failures, because no call was placed.",
    "Masks phone numbers in every displayed output, so a family's number does not reach an execution log.",
    "Discloses in the first three sentences of the call that the caller is an AI assistant and that a member of staff can be requested at any time.",
    "Caps concurrency at three and the wait for any one call at four minutes, because there is no cancel endpoint and the concurrency cap is the only brake that exists.",
    "Derives the Idempotency-Key from student and date, so a retry after a timeout cannot double-call a family."
  ],
  "credentials": [
    "CALL-E API key provided as CALL_E_API_KEY through the n8n deployment secret or environment-variable store. It is read at execution time and never stored in workflow data, so it does not travel in an export."
  ],
  "tested_with": {
    "node": "22.23.2"
  },
  "verification": {
    "unit_tests": "node --test examples/classify.test.mjs examples/workflow-shape.test.mjs, 21 passing on node 22.23.2. Name both files: `node --test examples/` resolves the directory as a module on this version and fails before it reads a test. The 14 classifier tests cover the three-outcome rule, the two shapes of an unusable answer, the refusal to borrow a task-level result on fan-out, number masking, and the attempt ledger. The 7 shape tests are described under structure_validated.",
    "generated": "examples/absence-wave.workflow.json is generated by examples/build-workflow.mjs, which inlines the tested classifier. Regeneration is byte-identical and one test fails if the workflow and the tested module drift apart.",
    "structure_validated": "examples/workflow-shape.test.mjs asserts the invariants n8n needs to load a workflow: the two collections it reads, the five fields each node is drawn from, unique names and ids, no connection naming a node that is absent, at least one trigger, no orphan node, and no credential baked into the file. Each of the five was checked by breaking the workflow that way and confirming the suite goes red: a renamed node with a stale connection, a duplicate name, a missing position, every trigger removed, and a live key inlined in a parameter. All five were caught, and the file was restored byte-identical.",
    "not_verified": "Validated is not imported. Nothing here has been loaded into a running n8n instance, so behaviour on import is unproven and the checks above only rule out the structural defects that stop an import. Reproduce with the command in unit_tests; an import screenshot would be the stronger evidence and this repository has no n8n to produce one."
  }
}
