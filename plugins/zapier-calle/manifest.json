{
  "name": "zapier-calle",
  "title": "Zapier CALL-E integration",
  "platform": "Zapier",
  "type": "platform-integration",
  "description": "Zapier Platform CLI integration that places CALL-E phone calls, waits for the terminal outcome through a Zapier callback URL, and returns a fail-closed disposition alongside the transcript, summary, and structured result.",
  "entrypoints": [
    {
      "name": "Place Call and Wait for Outcome",
      "path": "creates/place-call-and-wait.js"
    },
    {
      "name": "Start Call (No Wait)",
      "path": "creates/start-call.js"
    },
    {
      "name": "Find Call Result",
      "path": "searches/find-call-result.js"
    },
    {
      "name": "Call Completed",
      "path": "triggers/call-completed.js"
    }
  ],
  "side_effects": [
    "Creates one outbound phone call per Zap run when Dry Run is false, a valid CALL-E API key is connected, and an authorized E.164 phone number is supplied."
  ],
  "safety": [
    "Dry Run returns a masked preview, never generates a Zapier callback URL, and sends no request to CALL-E. It defaults to on, and an absent, blank, or unrecognized value is also a dry run, so placing a real call requires an explicit negative (false, 'false', 0, '0'). A field that was never set, one a Zap mapped from an empty upstream value, and one that predates this input existing all arrive identically, and none of them is a person choosing to dial: going live is an explicit act, so it takes an explicit value.",
    "Requires E.164 phone numbers and never infers region or locale from the number.",
    "Derives the Idempotency-Key from a SHA-256 digest of the canonical call payload, excluding the per-run webhook URL, so an edited retry cannot replay an older call.",
    "Classifies every outcome fail-closed: unrecognized statuses and event types resolve to needs_human, never to a successful disposition.",
    "Judges whether an extracted structured_result is usable, not merely present. A required field that came back as an unknown-like value (unknown, unclear, not_stated, undetermined), as null or empty, or that was never returned at all, resolves to review_required rather than confirmed - even when CALL-E reports task_completed with a high confidence label. This matters because CALL-E's own schema guidance tells authors to include an unknown enum value for calls that cannot produce evidence, and the sibling calle-script-advisor skill lints for it, so the value is expected to appear on a call that reached no answer. Booleans and zeros are treated as real answers, never as absences. The create actions check against the caller's own result_schema required list; the trigger and search, which observe calls placed anywhere, check the values they can see but cannot detect a missing field.",
    "Verifies an extracted answer against the transcript rather than trusting it. A result_schema may declare a <field>_quote string beside any field whose answer matters; the integration then checks that the quote actually appears in what the recipient said, and resolves to review_required when it does not - separately naming the case where the quote matches only a line the agent itself spoke, which means the model cited the question as evidence for the answer. This is the only check that reads the transcript rather than the model's own conclusions: every other gate can be satisfied by a confident, well-formed, entirely invented result. A field whose answer is itself unknown or empty is not asked for evidence, and a schema declaring no _quote fields is not checked at all, so the convention is opt-in. Matching is normalized substring containment, not semantic comparison, so an honest paraphrase is reported as ungrounded - it costs a human review rather than a wrong answer. When the call was placed with a result_schema, that schema - not the returned result - decides which quotes were owed, so a <field>_quote the schema declared and the extraction omitted is reported exactly as a blank one is; otherwise the check would fall silent on the one result that cited nothing. The two surfaces that classify a call placed elsewhere have no schema to read and can only verify the quotes present.",
    "Enforces a minimum numeric completion_confidence.score, defaulting to 0.6 and configurable per action, so a high confidence label carrying a low score is sent to review. CALL-E declares score as required and bounded 0-1 while label is documented only by example, making the label the looser field. An unparseable threshold falls back to the default rather than disabling the check; an explicit 0 disables it deliberately.",
    "Detects a revocation of consent spoken during the call by scanning the recipient's transcript turns, and never the agent's own script, which routinely contains the opt-out disclosure that state bot-disclosure laws encourage. A detected revocation overrides the business outcome: the call is reported as needs_human with lead_state blocked_compliance and is never actionable, regardless of how well the call otherwise went, and the quote and its offset into the call are surfaced so a human can verify it and add the number to a Do Not Call List. The FCC revocation rule accepts a request made by any reasonable means and requires it to be honored within 10 days. This is a phrase list over English text, not intent classification, and its ceiling is documented in lib/opt-out.js. Because this check reads the transcript out of the recipients array, a payload whose recipients are present but not readable as a list downgrades an otherwise-confirmed call to review_required rather than reporting opt_out_requested false: a check that silently examined nothing must not produce an actionable result.",
    "Refuses to dial when a caller-supplied history of previous attempt timestamps shows the call would exceed a per-day cap (default 2) or a minimum interval between attempts (default 4 hours), reported as its own disposition, retry_policy_blocked. The history is stateless - supplied fresh on each call, never stored - because a Zapier action has no durable storage. An unparseable or future-dated history fails closed and refuses to dial. Like the calling-window guard and unlike suppression, a dry run previews the verdict instead of being blocked by it.",
    "Reports attempts_total, the number of times a phone actually rang, summed across recipients. CALL-E redials on its own and nothing else in the response says how often, so a workflow that counts only the calls it placed will undercount and can deliver several times the attempt limit it promised.",
    "Projects the ten dispositions onto three coarse states in a lead_state field - qualified, needs_human, blocked_compliance - so a no-code workflow can branch without re-deriving which values are safe to act on. Only confirmed maps to qualified; an unrecognized disposition maps to needs_human, never to qualified.",
    "Surfaces the recipient's last spoken turn, with its offset into the call, as review_excerpt on any result that is not confirmed, so a human approval step shows the line that caused the doubt. The CALL-E API exposes no call recording or audio, so a timestamped transcript excerpt is the closest available equivalent.",
    "Distinguishes outcome_unknown from failed so an ambiguous result does not trigger a second call.",
    "Never classifies a webhook body. Both webhook surfaces - the resumable callback and the Call Completed trigger - receive an unsigned body on an unauthenticated URL, and CALL-E publishes no signing secret or subscription handshake, so nothing in the request proves its origin. Matching the call id does not help: the id travels inside the same untrusted body, so an id an attacker already knows is an id they can echo back, and every field after it - task_completed, the confidence, the structured result - would be whatever the sender typed. The delivered body is therefore treated as a notification naming a call, and the call is read back from CALL-E over the connection's own API key; that authenticated response is what gets classified. A lookup that fails resolves to needs_human with verified false, never to an actionable result. The Call Completed trigger additionally declines to fire at all when CALL-E has no such call on the connection, since that payload describes no call this account owns, while distinguishing that from a lookup that merely could not be completed - a briefly unreachable CALL-E must not silently swallow a real outcome. Every output carries verified, and Find Call Result reports it true unconditionally because it reads the call from CALL-E directly.",
    "Treats a webhook delivery as at-least-once and never actionable. Re-reading the call from CALL-E proves the record is authentic and belongs to the connection; it cannot prove the POST that named it was authorized or new, since anyone who learns the static webhook URL can send a known call id to it again with a fresh envelope id, and each send re-reads the same genuine result. Freshness is verified server-side: the outcome is dated by CALL-E's own completed_at, which arrives in the authenticated response rather than in the delivered body, and a result published more than 15 minutes earlier is reported as needs_human with notification_fresh false, so a replayed old success never arrives as confirmed. At-most-once cannot be verified on this surface at all - a duplicate inside that window is indistinguishable from a legitimate redelivery, and a Zapier trigger has no durable storage to remember a call id in - so the Call Completed trigger reports the full authenticated outcome for routing and review but never sets is_actionable true. Find Call Result, which reads from CALL-E directly over the connection's key, is the actionable path. The resumable callback behind Place Call and Wait needs neither mitigation: Zapier mints its URL per waiting run, resumes that run once, and the step rejects any body naming a different call.",
    "Rejects a resumed callback whose call id does not match the call the step started, before spending an API request on it. This is a filter, not authentication; the authenticated re-read is what makes the result trustworthy.",
    "Refuses to dial outside a configured calling window: when Recipient Timezone (IANA) is set, both create actions check the recipient's local hour (and optionally block Sunday) before any request is sent to CALL-E, and Place Call and Wait for Outcome never generates a Zapier callback URL for a call it refused. The timezone is never inferred from the phone number, region, or locale, and a raw UTC offset is rejected rather than accepted, per this repository's design-principles.md. This refusal is reported as its own disposition, outside_calling_window, distinct from the seven dispositions a completed call can produce - it fires before dialing and deriveDisposition never returns it - so a downstream Zap can tell a policy refusal apart from any outcome of an actual call. A dry run is never blocked by this guard: it always returns its masked preview, with a calling_window object reporting what the live path would have decided, so a Zap can be previewed at any hour.",
    "Masks phone numbers in previews, outputs, and error messages.",
    "Does not create recurring schedules; Zapier owns scheduling.",
    "The Call Completed trigger is an unauthenticated static webhook: CALL-E has no webhook subscription API, so a forged POST to a discovered URL is indistinguishable from a real CALL-E event at the transport layer. It fails closed on malformed payloads (returns no result rather than throwing), reuses the same disposition classifier as the create actions and search, and - because the transport cannot be authenticated - authenticates the content instead by reading the named call back from CALL-E before reporting anything. correlation_id is still an echoed value and is never proof of origin; downstream Zaps should branch on disposition.",
    "Refuses to dial a number on a caller-supplied Do Not Call List: both create actions check the recipient's number, digits-only and formatting-insensitive, against an optional per-call suppression list before any request is sent to CALL-E, and Place Call and Wait for Outcome never generates a Zapier callback URL for a call it refused this way. The list is stateless - supplied fresh on each call, never stored by this integration - and a list that cannot be read as text fails closed as suppressed rather than risking a dial. Unlike the calling-window guard, a dry run does not preview for a suppressed number, since suppression is about the number itself rather than timing. This refusal is reported as its own disposition, suppressed, distinct from the other nine - it fires before dialing and deriveDisposition never returns it."
  ],
  "credentials": [
    "CALL-E API key stored in the Zapier connection as a password-typed field."
  ],
  "tested_with": {
    "zapier-platform-core": "19.1.0"
  }
}
