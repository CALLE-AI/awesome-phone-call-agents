{
  "name": "provenance-grade",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Attach a knowledge grade — verified / asserted / assumed — to every field a CALL-E phone agent extracts, from the transcript turns the API already returns.",
  "scripts": {
    "test": "vitest run",
    "eval": "node scripts/eval.ts",
    "lint-task": "node scripts/lint-task.ts"
  },
  "dependencies": {
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
