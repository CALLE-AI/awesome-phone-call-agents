# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

playwright-report
test-results
.env
# Allow .env.mock — the offline demo mode config must be committed so that
# `npm run dev:mock` works out of the box without any manual setup.
!.env.mock