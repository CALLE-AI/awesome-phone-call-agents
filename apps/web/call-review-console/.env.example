# Optional. Only the webhook door needs these.
# Without them the console still runs: paste a payload, or paste a key.
# The webhook endpoint says it has nowhere to put events rather than dropping them.
KV_REST_API_URL=
KV_REST_API_TOKEN=
