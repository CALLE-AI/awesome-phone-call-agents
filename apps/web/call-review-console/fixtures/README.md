Fictional terminal call snapshots in the `GET /v1/calls/{call_id}` shape. Phone numbers are reserved fictional
numbers (+1 555 01xx). Nothing here was produced by a real call; the console runs on these by default.
