{
  "name": "calle-teamline",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Coach-authorized two-call workflow for gathering operational facts and communicating an approved decision.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check": "tsc --noEmit",
    "test": "node --import tsx --test test/*.test.ts",
    "start": "node --import tsx src/server.ts"
  },
  "dependencies": {
    "@call-e/calle": "0.2.2"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0"
  }
}
