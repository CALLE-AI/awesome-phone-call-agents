/** Hackathon demo: never auto-enqueue a follow-up CALL-E job. */
export const HACKATHON_RETRY_LOCKED = true;

export const AMBIGUOUS_OUTCOME_REASON =
  "Ambiguous provider outcome (failed/no-speech). Stopped for review — no automatic follow-up.";
