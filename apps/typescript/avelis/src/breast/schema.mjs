import {conversationSchema,evidenceSchema} from '../conversation-schema.mjs';
const text=description=>({type:'string',description});const choice=(values,description)=>({...text(description),enum:values});const obj=properties=>({type:'object',properties,required:Object.keys(properties),additionalProperties:false});const list=items=>({type:'array',items});
const fact=obj({code:text('Protocol topic or symptom attribute code from the supplied protocol.'),value:text('Use severity=mild/moderate/severe/unknown (patient wording belongs in detail); trend=improving/stable/persistent/worsening/unknown; boolean attributes=yes/no/unknown. Other symptom attributes are factual text. Missing or ambiguous answers are unknown; never infer a diagnosis.'),detail:text('Concise patient-reported detail, including change from last contact.'),evidence:evidenceSchema});
export const breastSchema={...conversationSchema,properties:{...conversationSchema.properties,instruction_results:{...conversationSchema.properties.instruction_results,items:{...conversationSchema.properties.instruction_results.items,properties:{...conversationSchema.properties.instruction_results.items.properties,order_quote:text('Exact excerpt from original clinician instruction OR exact question from supplied applicable protocol. Never paraphrase the source.')}}},schema_version:choice(['3'],'Breast cancer protocol follow-up.'),oncology:obj({respondent:choice(['patient','authorized_proxy','unknown'],'Proxy reports must never be represented as patient self-report.'),proxy_name:text('Exact preauthorized proxy name, or empty.'),phase_report:choice(['consistent','changed','unknown'],'Compare reported treatment with the loaded treatment context; do not infer stage or biomarkers.'),observations:{...list(fact),description:'Applicable protocol topic codes, plus visits.overdue, visits.unable and treatment.problem. These are NOT symptom attribute codes.'},symptoms:list(obj({name:choice(['pain','fever','breathing','arm_swelling','bleeding','hot_flash','adherence','other'],'Symptom router category; no diagnosis.'),facts:list(fact)})),limitations:list(text('Missing information, patient refusal, limited proxy scope, phase conflict or interrupted call.'))})},required:[...conversationSchema.required,'oncology']};
