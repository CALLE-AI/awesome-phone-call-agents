# Realtime boundary

Own the OpenAI Realtime session lifecycle, instructions and typed tool dispatch here. SPA-002 will add the local microphone harness after current API requirements are verified. Provider credentials must remain server-side; any browser session credential must be short-lived.
