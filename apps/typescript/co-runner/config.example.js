// Copy this file to config.js and fill in your real key. config.js is
// gitignored and must NEVER be committed — this file (config.example.js)
// is the only one that belongs in the repo.
const CALLE_CONFIG = {
    API_KEY: "PASTE_YOUR_CALLE_API_KEY_HERE", 
    BASE_URL: "https://api.heycall-e.com"
};