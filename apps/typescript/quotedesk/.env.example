# Required only for `run --rfq <id> --live`. Dry-run (the default) needs none of these.
CALLE_API_KEY=
# E.164 numbers for the four fictional distributors. Never committed, never logged;
# all output shows masked numbers only.
QUOTEDESK_PHONE_D1=
QUOTEDESK_PHONE_D2=
QUOTEDESK_PHONE_D3=
QUOTEDESK_PHONE_D4=
# Optional: company name spoken in the AI disclosure line.
QUOTEDESK_COMPANY=
