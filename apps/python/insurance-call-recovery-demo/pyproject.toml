[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "insurance-call-recovery-demo"
version = "0.1.0"
description = "Dry-run and CALL-E HTTP demo for the insurance-call-recovery Agent Skill"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
