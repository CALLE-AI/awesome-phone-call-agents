[project]
name = "calle-waitlist-slot-rescue-app"
version = "0.1.0"
description = "Consent-first waitlist slot rescue with sequential CALL-E calls."
requires-python = ">=3.11"
dependencies = [
  "calle-ai==0.2.0",
]

[dependency-groups]
dev = [
  "pytest>=9.0.0",
]
