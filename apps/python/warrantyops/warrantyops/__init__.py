"""One authorized, disclosed CALL-E call for a submitted warranty claim that
was rejected, returned, unpaid or stalled.

The package refuses most work it is offered: before the call, envelope,
residual-necessity, source-version, economic and authorization gates each can
refuse; behind the call, every asserted value must be grounded in a
counterparty transcript turn, a reference only survives its own read-back
exchange, and nothing is written back without a human approval bound to the
review packet against a source version that was just re-read.
"""

__all__ = ["__version__"]

__version__ = "1.0.0"
