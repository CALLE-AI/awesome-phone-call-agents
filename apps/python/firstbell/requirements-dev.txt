-r requirements.txt
pytest==9.1.1

# The page builder renders docs/ into pages so a reader who will not clone the
# repository can still open the two documents that decide whether it is usable in a
# school. Dev only: running firstbell itself needs nothing from this file.
markdown-it-py==4.2.0
