# Real-call artifacts

Transcripts, structured results, and timing logs from real CALL-E calls,
committed here so the "is it really calling, or just referencing?" question
never has to be taken on faith. Phone numbers are redacted before commit.

Populated during the demo phase — see the top-level README's Setup section
for how to place real calls.
