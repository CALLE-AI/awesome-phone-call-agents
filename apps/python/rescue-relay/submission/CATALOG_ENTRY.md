# Suggested app catalog entry

Add an entry matching the existing style of `apps/README.md`, linking to the new Python app directory. Preserve the repository’s current ordering and table columns.

**Rescue Relay** — Coordinate trusted animal-rescue contacts from a clarified goal to a safe outcome: compare complete capability/price offers, approve only the selected helpers, and track confirmed progress. Python/FastAPI; credential-free fictional demo; local-only CALL-E live mode.

Relative directory from `apps/README.md`: `python/rescue-relay/`

Do not put this application under plugins or skills: it is a standalone runnable web app, not an agent-installation package.

Ready-to-paste table row for the current catalog:

```markdown
| [`python/rescue-relay`](python/rescue-relay/) | Python / FastAPI | Clarifies an animal-rescue goal, compares complete capability/price offers, confirms only approved helpers through CALL-E, and tracks human-confirmed progress to safe closure. Includes a fictional demo. |
```
