# Only needed for --mode live. Never commit the real file.
CALLE_API_KEY=
# Optional: restrict live calls to these exact E.164 numbers (comma-separated).
# For hackathon verification, put your own test number here. Never commit it.
CLAIMCALL_ALLOWLIST=
