[project]
name = "genesis-call-e-orchestrator"
version = "0.1.0"
description = "Three safe, structured business phone workflows powered by CALL-E."
requires-python = ">=3.11"
dependencies = [
  "calle-ai==0.6.0",
]

[dependency-groups]
dev = [
  "pytest>=8,<10",
]
