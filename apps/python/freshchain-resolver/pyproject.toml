[project]
name = "freshchain-resolver"
version = "0.1.0"
description = "CALL-E cold-chain delivery exception resolver"
requires-python = ">=3.11"
dependencies = [
  "calle-ai==0.2.0",
  "tzdata>=2025.2; platform_system == 'Windows'",
]

[project.optional-dependencies]
dev = [
  "pytest>=8,<9",
]
