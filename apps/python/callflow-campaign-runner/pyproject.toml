[project]
name = "callflow-campaign-runner"
version = "0.1.0"
description = "Outbound calling campaigns through CALL-E with sentiment triage"
requires-python = ">=3.11"
dependencies = ["calle-ai>=0.6.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
